# FlapEmu Roadmap

**Branch:** `roadmap/library-decoupling`  
**Vision:** Import FlapEmu as a library; configure a board from JSON (`columns`, `presets`, `rows`, `ui`, etc). The page shell owns chrome, routing, and fetch.
